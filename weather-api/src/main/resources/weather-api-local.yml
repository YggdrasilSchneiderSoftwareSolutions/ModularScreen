openweathermap:
  # Hier API-Key definieren oder besser als Umgebungsvariable (wird geprüft siehe WeatherGatewayImpl)
  #api-key: xxxxxxxxx
  uri: /data/2.5/weather?zip={zipCode},{countryCode}&appid={openWeatherMapApiKey}&units={units}&lang={lang}